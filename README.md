# frp一键部署
frps一键部署并设置开机自启，执行命令 ：
```
wget -qO- https://raw.githubusercontent.com/eljefeZZZ/frp-auto-install/main/install_frp_server.sh | bash
```
```
curl -sSL https://raw.githubusercontent.com/eljefeZZZ/frp-auto-install/main/install_frp_server.sh | bash
```
